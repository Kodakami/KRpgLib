using KRpgLib.Utility.KomponentObject;

namespace KRpgLib.Items
{
    public interface IItemComponent : IKomponent
    {
        // Blank interface demonstrates intent. You can only register komponents decending from this type, not some other random KomponentObject's komponents.
    }
}
